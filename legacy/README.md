# Legacy Code

This folder keeps older code paths that are still worth preserving, but are no
longer the active working tree for the current stock-stack adapter.

Currently included:

- `python_baseline/`
  - the earlier standalone Python MPC baseline and MuJoCo prototype

