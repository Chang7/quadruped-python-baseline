# Trot Stable Compare

This folder contains the current email-ready trot proof assets.

## Purpose

- show that `linear_osqp` no longer collapses immediately in trot
- compare it against the stock sampling controller in the same short trot setup
- keep a compact visual package for email or slides

## Recommended Attachments

- `trot_stable_compare_table.png`
- `stock_sampling_trot_stable.gif`
- `linear_osqp_trot_stable.gif`

## Notes

- The GIF comparison uses a short same-scenario trot run so the visuals stay compact.
- The `linear_osqp` side also has a separate longer verification run:
  - `outputs/curated_runs/trot_after_crawl_balanced_default/episode_000/summary.json`

